<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('posts', function (Blueprint $table) {
            $table->id();
            $table->string('title');
            $table->string('slug');
            $table->string('short_title');
            $table->text('body');
            $table->unsignedBigInteger('post_type');
            $table->foreign('post_type')->references('id')->on('post_types');
            $table->unsignedBigInteger('iranic_media_type');
            $table->foreign('iranic_media_type')->references('id')->on('iranic_media_types');
            $table->json('related_posts')->nullable();
            $table->text('meta_description')->nullable();
            $table->boolean('hottest')->default(0);
            $table->boolean('main_hottest')->default(0);
            $table->boolean('special')->default(0);
            $table->tinyInteger('status')->default(2);
            $table->unsignedBigInteger('adder')->nullable();
            $table->foreign('adder')->references('id')->on('users');
            $table->unsignedBigInteger('editor')->nullable();
            $table->foreign('editor')->references('id')->on('users');
            $table->timestamps();
            $table->softDeletes();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('posts');
    }
};
